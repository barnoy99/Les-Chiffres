/* eslint-disable */
/* ============================================================
   firebase-config.js — Les Chiffres
   This is a PLACEHOLDER. The app runs on localStorage only until
   you paste a real config from your OWN, SEPARATE Firebase project
   (do NOT reuse Le-Francais-au-Quotidien's project — see
   FIREBASE_SETUP.md). While apiKey === 'YOUR_API_KEY', app.js
   skips Firebase and uses localStorage only.
   ============================================================ */
var FIREBASE_CONFIG = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'YOUR_PROJECT.firebaseapp.com',
  databaseURL: 'https://YOUR_PROJECT-default-rtdb.firebaseio.com',
  projectId: 'YOUR_PROJECT',
  storageBucket: 'YOUR_PROJECT.appspot.com',
  messagingSenderId: 'YOUR_SENDER_ID',
  appId: 'YOUR_APP_ID'
};
